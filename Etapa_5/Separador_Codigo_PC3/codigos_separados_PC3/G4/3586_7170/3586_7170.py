from numpy import *

v = array(eval(input("Aneis: ")))
p = 0
i = 0

while v[i] >= 1:
	for v[i] in [1]:
		p = p + 100
	for v[i] in [2]:
		p = p + 60
	for v[i] in [3]:
		p = p + 20
	for v[i] in [4]:
		p = p + 0
	i += 1
print(p)