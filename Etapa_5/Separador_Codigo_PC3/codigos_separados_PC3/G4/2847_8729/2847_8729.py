from numpy import*

n = array(eval(input("numero a ser substituido: ")))

z = zeros(size(n),dtype=int)
e = 0

for i in range(size(n)):
	if n[e]==0:
		z[e]==9
	else:
		z[e] = n[e] ** 2
	e = e+1
print(z)