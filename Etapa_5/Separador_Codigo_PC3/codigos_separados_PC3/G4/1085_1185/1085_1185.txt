======================================================================
RELATÓRIO DE ANÁLISE DE MISCONCEPTIONS (MC³)
======================================================================
Arquivo  : 1085_1185.py
Questão  : 1085
Aluno    : 1185
Total MCs: 1

⚠️  Misconceptions detectados: G4

----------------------------------------------------------------------
CÓDIGO FONTE
----------------------------------------------------------------------
     1 | a = float(input("Qual a nota da primeira prova? "))
     2 | b = float(input("Qual a nota da segunda prova? "))
     3 | c = float(input("Qual a nota da terceira prova? "))
     4 | d = float(input("Qual a nota da quarta prova? "))
     5 | e = float(input("Qual a nota da quinta prova? "))
     6 | m = round((a + b + c + d + e)/ 5,2)
     7 | if(m >= 6):
     8 | 	print(m)	
     9 | 	print("Aprovado")
    10 | else:
    11 | 	print(m)
    12 | 	print("Reprovado")
    13 | 	

----------------------------------------------------------------------
DETALHAMENTO DOS MISCONCEPTIONS
----------------------------------------------------------------------

▶ G4 — Funções/variáveis com nomes não significativos
  Ocorrências: 6

  [1] Linha 1: Variável 'a' com nome muito curto (≤4 chars)
      Trecho  : a = ...
      Contexto:
        >>>    1 | a = float(input("Qual a nota da primeira prova? "))
               2 | b = float(input("Qual a nota da segunda prova? "))

  [2] Linha 2: Variável 'b' com nome muito curto (≤4 chars)
      Trecho  : b = ...
      Contexto:
               1 | a = float(input("Qual a nota da primeira prova? "))
        >>>    2 | b = float(input("Qual a nota da segunda prova? "))
               3 | c = float(input("Qual a nota da terceira prova? "))

  [3] Linha 3: Variável 'c' com nome muito curto (≤4 chars)
      Trecho  : c = ...
      Contexto:
               2 | b = float(input("Qual a nota da segunda prova? "))
        >>>    3 | c = float(input("Qual a nota da terceira prova? "))
               4 | d = float(input("Qual a nota da quarta prova? "))

  [4] Linha 4: Variável 'd' com nome muito curto (≤4 chars)
      Trecho  : d = ...
      Contexto:
               3 | c = float(input("Qual a nota da terceira prova? "))
        >>>    4 | d = float(input("Qual a nota da quarta prova? "))
               5 | e = float(input("Qual a nota da quinta prova? "))

  [5] Linha 5: Variável 'e' com nome muito curto (≤4 chars)
      Trecho  : e = ...
      Contexto:
               4 | d = float(input("Qual a nota da quarta prova? "))
        >>>    5 | e = float(input("Qual a nota da quinta prova? "))
               6 | m = round((a + b + c + d + e)/ 5,2)

  [6] Linha 6: Variável 'm' com nome muito curto (≤4 chars)
      Trecho  : m = ...
      Contexto:
               5 | e = float(input("Qual a nota da quinta prova? "))
        >>>    6 | m = round((a + b + c + d + e)/ 5,2)
               7 | if(m >= 6):

======================================================================