from numpy import*
vet = array(eval(input("Primeiro vetor: ")))

while (size(vet) > 1):
   
   npar = 0

   
   for elemento in vet:
      if (elemento % 2 == 0):
         npar = npar + 1
			
		
      
vet = array(eval(input("Proximo vetor: ")))

