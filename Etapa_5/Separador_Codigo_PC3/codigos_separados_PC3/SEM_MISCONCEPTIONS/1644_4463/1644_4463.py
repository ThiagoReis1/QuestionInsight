from numpy import *

notas = array(eval(input("Notas dos alunos: ")))

r = 0
 
v = zeros(5, dtype=int)

for i in range(size(notas)):
	if(notas[i] >= 0.0 and notas[i] < 5.0):
		r = r + 1
		v[i] = i
	
v2 = zeros(size(r), dtype=int)

for i in range(size(r)):
	if(v[])
print(r)
print(v2)

			


