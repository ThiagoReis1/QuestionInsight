from numpy import *

a = array(eval(int(input()))
vetor = zeros(size(a),dtype=int)
cont = 0
			 
for i in range(size(a)):
	b[i] = a[i] > 2000
	vetor = zeros(size(a),dtype=int)>2000		 
	for i in range(size(a)):
		cont = cont + 1
			 
cont += 1

print(cont)
	