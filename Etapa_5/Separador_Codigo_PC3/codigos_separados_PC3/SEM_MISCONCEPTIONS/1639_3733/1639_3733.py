from numpy import *

elementos=array(eval(input("alunos: ")))
turmas = 0
alunos = zeros(dtype=int)

while(size,vet(0)):
	for vet in vet
		if(turmas=alunos):
			turmas= turmas + 1
		else (alunos>turmas):
			alunos= alunos = 1
	for vet in vet
		