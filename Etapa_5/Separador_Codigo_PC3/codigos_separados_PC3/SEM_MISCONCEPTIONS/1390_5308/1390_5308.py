consumo = float(input(" consumo de minutos? "))

inteiro = int(consumo)


if ( inteiro <= 100):
	tarifa = consumo * 1.20
	
	print(round(tarifa, 2))
	
else:
	(inteiro > 100)
	tarifa = consumo * 1.40
	soma = tarifa + 25
	
	print(round(soma, 2))
	