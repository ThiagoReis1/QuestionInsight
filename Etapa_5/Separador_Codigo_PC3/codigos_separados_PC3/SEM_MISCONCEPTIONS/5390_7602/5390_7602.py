from numpy import*

etiqueta = input().upper()

i = 0
cont = 0

while i < len(etiqueta):
	if etiqueta[i] == "A" or "E" or "I" or "O" or "U":
		cont 
	else:
		cont += 0.23
	i += 1

print(round(cont,2))