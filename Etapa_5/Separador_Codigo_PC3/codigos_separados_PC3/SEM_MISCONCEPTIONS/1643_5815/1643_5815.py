from numpy import*

#Alunos aprovados na disciplina
#vetor contendo o valor dos índices do vetor de entrada
#usar a função zeros(N, dtype = int )

nota= array(eval(input("Qual a nota do aluno: ")))
aprovadodis= 0
reprovadodis=0
#Aprovado >= 5.0

for i 
  if nota >= 5:
		aprovadodis= aprovadodisc+ 1
  else:
		reprovad