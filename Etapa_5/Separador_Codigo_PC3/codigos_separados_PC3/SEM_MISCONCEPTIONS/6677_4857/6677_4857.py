from numpy import*

vt = 0
t = zeros(10, dtype = int)

for i in range(10):
	t[i] = float(input())
a = int(input("DIgite o valor: "))
for i in range(10):
	if [i] > t:
		vt = vt +1
vet = zeros(vt, dtype = float)
    if t[i] >= t:
		vet[i] = t[i]
		
print(t)
print(vet)

