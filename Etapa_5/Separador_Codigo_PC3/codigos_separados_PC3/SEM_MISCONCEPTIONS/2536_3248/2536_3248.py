C = float(input("insira o valor da casa:"))
D = float(input("Quantia inicial: "))
M = float(input("deposito mensal: "))
j = float(input("juros: "))
pj = j/100
saldo = D     # Variavel acumuladora

# Valor inicial da variavel contadora(tempo)
t = 0

# Atualizacao de saldo
while()
((C > 0) and (D > 0) and (M > 0) and (j > 0)):
	rend = saldo * j / 100
	saldo = saldo + rend
	t = t + 1
	print(t)
	
	if((C < 0) or (D < 0) or (M < 0) or (j < 0)):
		print("Dados incorretos")
	
	
	
