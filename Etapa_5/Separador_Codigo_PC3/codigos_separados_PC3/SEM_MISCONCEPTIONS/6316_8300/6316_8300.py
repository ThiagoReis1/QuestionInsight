def cacular_compra(string):
	preco_doces = 2.25
	preco_salgados = 4.00
	preco_integrais = 6.90
	
	total_doces = 0
	total_salgados = 0
	total_integrais = 0
	
	for produto in string:
		if produto == "D"
			total_doces += 1
		elif produto == "S"
			total_salgados += 1
		elif produto == "I"
		total_integrais += 1
		valor_total=(total_doces * preco_doces) + (total_salgados * preco_salgados) + (total_integrais * preco_integrais)
		return round (valor_total, 2),total_doces, total_salgados, total_integrais
	string = input("Digite ")
	
