lado_a = float(input("qual medida de a:"))

lado_b = float(input("qual medida de b"))

lado_c = float(input("qual medida de c:"))

constr = float(input(" qual o custo da construção por metro:"))

print(round((lado_a + lado_b + lado_c) * constr, 2))

