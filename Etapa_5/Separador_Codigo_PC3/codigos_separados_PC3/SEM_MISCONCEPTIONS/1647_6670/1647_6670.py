from numpy import *

freq=array(eval(input()))
acum=0
i=0

saida=zeros(freq,dtype=int)

#while i > size(freq):
#	if freq>=70:
		acum[i]=saida
		acum+=1
for j in size(freq):
	if freq[j] >= 70:
		acum+=1
print(j)