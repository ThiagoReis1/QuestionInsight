macaco = 1.4
taxam = 0.06

leao = float(input("digite:"))
taxal = float(input("digite:"))
anos = 0

cleao = leao + taxal * anos
cmacaco = macaco + taxam * anos

while (cleao > cmacaco):
	cleao = leao + taxal * anos
	cmacaco = maca