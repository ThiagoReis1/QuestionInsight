from numpy import *
temp = array(eval(input("Temperaturas: ")))
l = shape(temp)[0]
c = shape(temp)[1]
saida = zeros((1,3))
for i in range(l):
	for j in range(c):
		saida [0,j] = min.temp[0,:]
   	saida [0,j] = min.temp[1,:]
		print(saida)