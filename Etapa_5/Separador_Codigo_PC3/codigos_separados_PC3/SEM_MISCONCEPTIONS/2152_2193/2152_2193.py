from numpy import*
vet = array(eval(input("Primeiro vetor: ")))
m = zeros(n, dtype=int)
n = 0
for elemento in vet:
	if (elemento % 2 != 0):
		n = n + 1

i = array(eval(input(zeros(n, dtype=int))))
for j in i:
	if (elemento )

