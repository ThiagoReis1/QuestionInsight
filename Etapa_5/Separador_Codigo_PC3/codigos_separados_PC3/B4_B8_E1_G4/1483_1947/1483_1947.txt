======================================================================
RELATÓRIO DE ANÁLISE DE MISCONCEPTIONS (MC³)
======================================================================
Arquivo  : 1483_1947.py
Questão  : 1483
Aluno    : 1947
Total MCs: 4

⚠️  Misconceptions detectados: B4, B8, E1, G4

----------------------------------------------------------------------
CÓDIGO FONTE
----------------------------------------------------------------------
     1 | 
     2 | ent=input("Nome do equipamento:").upper()
     3 | qtd=int(input("Quantidade a ser transportada:"))
     4 | 
     5 | if(qtd < 0 or qtd > 1000):
     6 | 	print("Entrada invalida")
     7 | elif(ent == "COMPUTADOR" or ent == "FREEZER" or ent == "FURADEIRA" or ent == "LIQUIDIFICADOR" or ent == "MICROONDAS" or ent == "NOTEBOOK" or ent == "TELEVISOR" or ent == "VENTILADOR"):
     8 | 	if(ent == "COMPUTADOR"):
     9 | 		peso=12
    10 | 		pesototal=peso * qtd
    11 | 		print(round(pesototal, 2))
    12 | 	elif(ent == "FREEZER"):
    13 | 		peso=52
    14 | 		pesototal=peso * qtd
    15 | 		print(round(pesototal, 2))
    16 | 	elif(ent == "FURADEIRA"):
    17 | 		peso=1.7
    18 | 		pesototal=peso * qtd
    19 | 		print(round(pesototal, 2))
    20 | 	elif(ent == "LIQUIDIFICADOR"):
    21 | 		peso=1.8
    22 | 		pesototal=peso * qtd
    23 | 		print(round(pesototal, 2))
    24 | 	elif(ent == "MICROONDAS"):
    25 | 		peso=15
    26 | 		pesototal=peso * qtd
    27 | 		print(round(pesototal, 2))
    28 | 	elif(ent == "NOTEBOOK"):
    29 | 		peso=2.5
    30 | 		pesototal=peso * qtd
    31 | 		print(round(pesototal, 2))
    32 | 	elif(ent == "TELEVISOR"):
    33 | 		peso=15
    34 | 		pesototal=peso * qtd
    35 | 		print(round(pesototal, 2))
    36 | 	elif(ent == "VENTILADOR"):
    37 | 		peso=2.4
    38 | 		pesototal=peso * qtd
    39 | 		print(round(pesototal, 2))
    40 | elif(ent != "COMPUTADOR" and ent != "FREEZER" and ent != "FURADEIRA" and ent != "LIQUIDIFICADOR" and ent != "MICROONDAS" and ent != "NOTEBOOK" and ent != "TELEVISOR" and ent != "VENTILADOR"):
    41 | 	print("Entrada invalida")

----------------------------------------------------------------------
DETALHAMENTO DOS MISCONCEPTIONS
----------------------------------------------------------------------

▶ B4 — Comandos repetidos dentro de blocos if-elif-else
  Ocorrências: 6

  [1] Linha 40: Bloco idêntico ao da linha 5
      Trecho  : print('Entrada invalida')
      Contexto:
              39 | 		print(round(pesototal, 2))
        >>>   40 | elif(ent != "COMPUTADOR" and ent != "FREEZER" and ent != "FURADEIRA" and ent != "LIQUIDIFICADOR" and ent != "MICROONDAS" and ent != "NOTEBOOK" and ent != "TELEVISOR" and ent != "VENTILADOR"):
              41 | 	print("Entrada invalida")

  [2] Linha 32: Bloco idêntico ao da linha 24
      Trecho  : peso = 15
      Contexto:
              31 | 		print(round(pesototal, 2))
        >>>   32 | 	elif(ent == "TELEVISOR"):
              33 | 		peso=15

  [3] Linha 32: Bloco idêntico ao da linha 24
      Trecho  : peso = 15
      Contexto:
              31 | 		print(round(pesototal, 2))
        >>>   32 | 	elif(ent == "TELEVISOR"):
              33 | 		peso=15

  [4] Linha 32: Bloco idêntico ao da linha 24
      Trecho  : peso = 15
      Contexto:
              31 | 		print(round(pesototal, 2))
        >>>   32 | 	elif(ent == "TELEVISOR"):
              33 | 		peso=15

  [5] Linha 32: Bloco idêntico ao da linha 24
      Trecho  : peso = 15
      Contexto:
              31 | 		print(round(pesototal, 2))
        >>>   32 | 	elif(ent == "TELEVISOR"):
              33 | 		peso=15

  [6] Linha 32: Bloco idêntico ao da linha 24
      Trecho  : peso = 15
      Contexto:
              31 | 		print(round(pesototal, 2))
        >>>   32 | 	elif(ent == "TELEVISOR"):
              33 | 		peso=15

▶ B8 — Não utilização de elif/else
  Ocorrências: 2

  [1] Linha 7: Cadeia if/elif sem bloco else final
      Trecho  : if ... (linha 7) / elif ... sem else
      Contexto:
               6 | 	print("Entrada invalida")
        >>>    7 | elif(ent == "COMPUTADOR" or ent == "FREEZER" or ent == "FURADEIRA" or ent == "LIQUIDIFICADOR" or ent == "MICROONDAS" or ent == "NOTEBOOK" or ent == "TELEVISOR" or ent == "VENTILADOR"):
               8 | 	if(ent == "COMPUTADOR"):

  [2] Linha 32: Cadeia if/elif sem bloco else final
      Trecho  : if ... (linha 32) / elif ... sem else
      Contexto:
              31 | 		print(round(pesototal, 2))
        >>>   32 | 	elif(ent == "TELEVISOR"):
              33 | 		peso=15

▶ E1 — Verificação desnecessária de todas as combinações possíveis
  Ocorrências: 2

  [1] Linha 7: 8 condições em um único if — verificação excessiva
      Trecho  : ent == 'COMPUTADOR' or ent == 'FREEZER' or ent == 'FURADEIRA' or (ent == 'LIQUIDIFICADOR') or (ent == 'MICROONDAS') or (ent == 'NOTEBOOK') or (ent == 'TELEVISOR') or (ent == 'VENTILADOR')
      Contexto:
               6 | 	print("Entrada invalida")
        >>>    7 | elif(ent == "COMPUTADOR" or ent == "FREEZER" or ent == "FURADEIRA" or ent == "LIQUIDIFICADOR" or ent == "MICROONDAS" or ent == "NOTEBOOK" or ent == "TELEVISOR" or ent == "VENTILADOR"):
               8 | 	if(ent == "COMPUTADOR"):

  [2] Linha 40: 8 condições em um único if — verificação excessiva
      Trecho  : ent != 'COMPUTADOR' and ent != 'FREEZER' and (ent != 'FURADEIRA') and (ent != 'LIQUIDIFICADOR') and (ent != 'MICROONDAS') and (ent != 'NOTEBOOK') and (ent != 'TELEVISOR') and (ent != 'VENTILADOR')
      Contexto:
              39 | 		print(round(pesototal, 2))
        >>>   40 | elif(ent != "COMPUTADOR" and ent != "FREEZER" and ent != "FURADEIRA" and ent != "LIQUIDIFICADOR" and ent != "MICROONDAS" and ent != "NOTEBOOK" and ent != "TELEVISOR" and ent != "VENTILADOR"):
              41 | 	print("Entrada invalida")

▶ G4 — Funções/variáveis com nomes não significativos
  Ocorrências: 1

  [1] Linha 0: G4 detectado pelo VisitorMC3 (localização exata não mapeada)
      Trecho  : —

======================================================================