======================================================================
RELATÓRIO DE ANÁLISE DE MISCONCEPTIONS (MC³)
======================================================================
Arquivo  : 5255_7671.py
Questão  : 5255
Aluno    : 7671
Total MCs: 2

⚠️  Misconceptions detectados: B8, G4

----------------------------------------------------------------------
CÓDIGO FONTE
----------------------------------------------------------------------
     1 | #Ecercicio dos Correios
     2 | 
     3 | p = float(input("Qual o peso do produto? "))
     4 | 
     5 | d = float(input("Qual a distancia do local origem para o destino? "))
     6 | 
     7 | c = int(input("Qual o numero do codigo? "))
     8 | 
     9 | if(c == 1):
    10 | 	total = ((p * 25 + d * 0.10) * (1.0 + 17 / 100))
    11 | 	print(round(total,2))
    12 | elif(c == 2):
    13 | 	total = ((p * 25 + d * 0.10) * (1.0 + 17.5 / 100))
    14 | 	print(round(total,2))
    15 | elif(c == 3):
    16 | 	total = ((p * 25 + d * 0.10) * (1.0 + 18 / 100))
    17 | 	print(round(total,2))
    18 | elif(c == 4):
    19 | 	total = ((p * 25 + d * 0.10) * (1.0 + 20 / 100))
    20 | 	print(round(total,2))

----------------------------------------------------------------------
DETALHAMENTO DOS MISCONCEPTIONS
----------------------------------------------------------------------

▶ B8 — Não utilização de elif/else
  Ocorrências: 1

  [1] Linha 15: Cadeia if/elif sem bloco else final
      Trecho  : if ... (linha 15) / elif ... sem else
      Contexto:
              14 | 	print(round(total,2))
        >>>   15 | elif(c == 3):
              16 | 	total = ((p * 25 + d * 0.10) * (1.0 + 18 / 100))

▶ G4 — Funções/variáveis com nomes não significativos
  Ocorrências: 1

  [1] Linha 0: G4 detectado pelo VisitorMC3 (localização exata não mapeada)
      Trecho  : —

======================================================================