from numpy import *
p = float(input("numero real"))
x = eval(input("valores x: "))
y = eval(input("valores y: "))
z = zeros(size(x))
for i in range(x) and range(y):
	t = p / (p + 1)
	diferenca = x - 2 * y
	print(diferenca)