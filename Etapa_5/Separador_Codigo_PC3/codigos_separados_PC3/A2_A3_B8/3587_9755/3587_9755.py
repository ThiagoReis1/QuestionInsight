from numpy import *

v = array(eval(input("digite o valor: ")))

i = 0
pontos = 100

while i <= 0:
	if v[i] == 1:
		pontos = pontos*5
		
	elif v[i] == 2:
		pontos = pontos*3
		
	elif v[i] == 3:
		pontos = pontos
		
	elif v[i] == 4:
		pontos = pontos/2
		 
	valor = t1*t2*t3*t4
	i = i+1
	
	print(round(total, 2))