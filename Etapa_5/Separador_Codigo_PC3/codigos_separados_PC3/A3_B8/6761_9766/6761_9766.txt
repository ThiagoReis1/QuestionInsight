======================================================================
RELATÓRIO DE ANÁLISE DE MISCONCEPTIONS (MC³)
======================================================================
Arquivo  : 6761_9766.py
Questão  : 6761
Aluno    : 9766
Total MCs: 2

⚠️  Misconceptions detectados: A3, B8

----------------------------------------------------------------------
CÓDIGO FONTE
----------------------------------------------------------------------
     1 | # faça seu código aqui!
     2 | 
     3 | velocidade = float(input("insira o valor velocidade: "))
     4 | custo_total_assinatura = float(input("insira o valor do custo_total_assinatura: "))
     5 | custo_fixo = float(input("insira o valor de custo_fixo: "))
     6 | assinatura = float(input("digite a assinatura: "))
     7 | fixo = float(input("digite o fixo: "))
     8 | conta = float(input("digite o valor conta: "))
     9 | 
    10 | if velocidade < 50:
    11 | 	print("4.50")
    12 | elif velocidade == 50:
    13 | 	print("5.50")
    14 | elif velocidade > 50:
    15 | 	print("6.50")
    16 | conta = (custo_total + custo_fixo) / 4.00
    17 | conta = (custo_total + custo_fixo) / 5.50
    18 | conta = (custo_total + custo_fixo) / 6.50
    19 | 
    20 | print("valor total")

----------------------------------------------------------------------
DETALHAMENTO DOS MISCONCEPTIONS
----------------------------------------------------------------------

▶ A3 — Variável inicializada desnecessariamente
  Ocorrências: 4

  [1] Linha 4: Variável 'custo_total_assinatura' declarada mas nunca utilizada
      Trecho  : custo_total_assinatura = ...
      Contexto:
               3 | velocidade = float(input("insira o valor velocidade: "))
        >>>    4 | custo_total_assinatura = float(input("insira o valor do custo_total_assinatura: "))
               5 | custo_fixo = float(input("insira o valor de custo_fixo: "))

  [2] Linha 6: Variável 'assinatura' declarada mas nunca utilizada
      Trecho  : assinatura = ...
      Contexto:
               5 | custo_fixo = float(input("insira o valor de custo_fixo: "))
        >>>    6 | assinatura = float(input("digite a assinatura: "))
               7 | fixo = float(input("digite o fixo: "))

  [3] Linha 7: Variável 'fixo' declarada mas nunca utilizada
      Trecho  : fixo = ...
      Contexto:
               6 | assinatura = float(input("digite a assinatura: "))
        >>>    7 | fixo = float(input("digite o fixo: "))
               8 | conta = float(input("digite o valor conta: "))

  [4] Linha 8: Variável 'conta' declarada mas nunca utilizada
      Trecho  : conta = ...
      Contexto:
               7 | fixo = float(input("digite o fixo: "))
        >>>    8 | conta = float(input("digite o valor conta: "))
               9 | 

▶ B8 — Não utilização de elif/else
  Ocorrências: 1

  [1] Linha 12: Cadeia if/elif sem bloco else final
      Trecho  : if ... (linha 12) / elif ... sem else
      Contexto:
              11 | 	print("4.50")
        >>>   12 | elif velocidade == 50:
              13 | 	print("5.50")

======================================================================