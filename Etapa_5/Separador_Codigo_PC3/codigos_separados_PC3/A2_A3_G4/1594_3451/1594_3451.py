from numpy import*
from numpy.linalg import*
m=array(eval(input("")))
b=m
p=0
a=1
soma=0
for i in range(shape(m)[0]):
	for j in range(shape(m)[0]):
		while(size(m)==3):
			p=p+1
			a=(a+1)
			soma=soma
		
		
print(soma)

	